[tool.ruff]
line-length = 130
target-version = "py310"
exclude = ["venv", ".venv", "migrations"]
ignore = ["I001"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["app"]
